%li.nav-item#search
  = form_tag search_path, method: 'get', id: 'search-form' do |f|
    .input-prepend
      .add-on
        %i.icon-search
      = text_field_tag :query, params[:query], label: false, placeholder: " search", class: 'input-large'
