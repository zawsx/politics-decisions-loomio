%h1 Searches#show

.inbox-container
  - @search.results.group_by(&:group).each_pair do |group, items|
    .inbox-group
      %h2= link_to group.full_name, group
      %ul.inbox-list
        - items.each do |item|
          -if item.is_a? Discussion
            =render 'discussion_line_item', item: item
          -if item.is_a? Motion
            =render 'motion_line_item', item: item

